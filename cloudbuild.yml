steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'custom-yarn', '.']

- name: 'custom-yarn'
  args: ['install']
  dir: 'image'
  id: 'image-install'

# - name: 'gcr.io/cloud-builders/yarn'
#   args: ['install']
#   dir: 'pose'
#   id: 'pose-install'
#   waitFor: ['-']


# - name: 'gcr.io/cloud-builders/yarn'
#   args: ['install']
#   dir: 'audio'
#   id: 'audio-install'
#   waitFor: ['-']


# - name: 'tester'
- name: 'custom-yarn'
  args: ['run', 'test']
  dir: 'image'
  # waitFor: ['image-install']

# options:
  # machineType: 'N1_HIGHCPU_8'
